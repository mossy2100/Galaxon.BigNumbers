Console.WriteLine("Hello, World.");

// for (double x = 1.0; x <= 2.0; x += 0.1)
// {
//     int y = (int)x;
//     Console.WriteLine($"{x} cast to int gives {y}");
// }

// We can't get the principal square root of a negative number as a real value.

// double x = -2;
// double y = 0.5;
//
// // But x^n/d =
//
// int d = int.MaxValue - 1;
// int n = (int)Math.Round(d * y);
// double sqrt = double.Pow(double.RootN(x, d), n);
// Console.WriteLine($"{x}^{y} = {sqrt}");
